GraphQL is a powerful query language used to communicate data between a server and the client and it
allows us to structure data driven application in a more flexible and efficient way than REST.

Install following packages:

express, nodemon, graphql, express-graphql

Install the following packages in frontend:
apollo-boost react-apollo graphql